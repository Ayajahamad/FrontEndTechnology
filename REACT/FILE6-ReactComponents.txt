REACT-COMPONENTS
    React component is a javascript function that you can sprinkle with Markup.

    Let's deep dive into writing our first component.
    1. Export the component.
    2. Defining the function.
    3. Implementing the markup.
    4. Call the component.

    NOTE : Never define component under another component.

    1. export default function functionName(){
        return(
            MARKUP
        );
    }
    2. You can't write a component inside another component.
    3. The first alphabet of the component function name should be uppercase.